# Gradient Descent

```bash
math-anim plan \
  --formula "\\theta_{t+1} = \\theta_t - \\eta \\nabla L(\\theta_t)" \
  --goal "勾配降下法を3D曲面で理解したい" \
  --output-dir outputs/gradient_descent_plan \
  --no-llm \
  --to-storyboard
```
